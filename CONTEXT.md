# HTTP

This context turns an HTTP request into a matched endpoint invocation and an
HTTP response without exposing framework-specific routing details.

## Language

**Route Parameter**:
An application value captured from a route placeholder in either the URI path
or host.
_Avoid_: Path parameter when referring to the combined set of path- and
host-derived values
